# Copy this file to `.env` and fill in the value.
# `.env` is gitignored so the key stays on disk, out of the repo.
# The app reads this automatically at startup (main/index.ts loads dotenv).

OPENAI_API_KEY=sk-proj-...
